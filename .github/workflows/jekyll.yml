# This workflow builds and deploys a Jekyll site to GitHub Pages.
# It always builds from the `main` branch, regardless of which branch was pushed.
# Triggers: pushes to `main`.

name: Deploy Jekyll site to Pages

on:
  push:
    branches:
      - main
  repository_dispatch:
    types: [build_web]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main branch (always)
        uses: actions/checkout@v5
        with:
          ref: main

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1 # v1.313.0
        with:
          ruby-version: '3.1'
          bundler-cache: true
          cache-version: 0

      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v6

      - name: Build with Jekyll
        run: bundle exec jekyll build --baseurl "${{ steps.pages.outputs.base_path }}"
        env:
          JEKYLL_ENV: production

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v5

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
